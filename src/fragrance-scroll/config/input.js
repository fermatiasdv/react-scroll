// Constantes de gestos y teclas, con los valores del legacy (L:scroll-config.js, RF-06).

/** Distancia vertical (px) que tiene que recorrer el dedo para contar como swipe. */
export const SWIPE_MIN_DISTANCE_PX = 60

/** Cuánto más vertical que horizontal tiene que ser un gesto para contarlo como swipe. */
export const SWIPE_DIRECTION_RATIO = 1.2

// AJUSTE-01: agrupación de gestos de rueda y teclado, sólo para probar en el sandbox. Ver docs/ajustes.md.
export const GESTURE_GAP_MS = 250

// AJUSTE-01: teclas de desarrollo, sólo para probar en el sandbox. Ver docs/ajustes.md.
export const NEXT_KEYS = ['ArrowDown', 'PageDown', ' ']
export const PREV_KEYS = ['ArrowUp', 'PageUp']
